<div class="container">
	<h2>ジャンル編集</h2>
	<div class="row">
		<%= form_for @genre, url: admins_genre_path do |f| %>
	 		<%= f.label :ジャンル %>
	 	 	<%= f.text_field :name %>
	 	 	<%= f.submit "変更を保存する" %>
	 	<% end %>
	</div>
</div>
