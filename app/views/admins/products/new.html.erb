<div class="container">
	<div class="row">
		<div class="col-xs-8">
			<%= form_for(@product) do |f| %>
				<div>
				<%= f.label :商品名 %>
				<%= f.text_field :name %>
				</div>
				<div>
				<%= f.submit "商品登録" %>
				</div>
			<% end %>
		</div>
	</div>
</div>